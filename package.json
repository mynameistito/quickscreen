{
  "name": "quickscreen",
  "version": "0.1.1",
  "description": "CLI tool to arrange windows into predefined layouts and record the screen with optional microphone audio — supports macOS and Windows",
  "type": "module",
  "main": "./dist/cli.js",
  "types": "./dist/cli.d.ts",
  "bin": {
    "quickscreen": "./dist/cli.js"
  },
  "exports": {
    "./package.json": "./package.json",
    ".": {
      "types": "./dist/cli.d.ts",
      "default": "./dist/cli.js"
    },
    "./src": {
      "types": "./src/cli.ts",
      "default": "./src/cli.ts"
    },
    "./src/*": {
      "types": "./src/*.ts",
      "default": "./src/*.ts"
    }
  },
  "files": [
    "src",
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc && node -e \"try{require('fs').chmodSync('dist/cli.js',0o755)}catch(_){}\"",
    "dev": "bun src/cli.ts",
    "prepublishOnly": "bun run build"
  },
  "keywords": [
    "screen-recording",
    "ffmpeg",
    "macos",
    "windows",
    "window-layout",
    "avfoundation",
    "gdigrab",
    "cli"
  ],
  "dependencies": {
    "goke": "^6.3.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^22.14.1"
  }
}
